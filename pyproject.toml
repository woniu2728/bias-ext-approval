[project]
name = "bias-ext-approval"
version = "0.1.0"
dependencies = ["bias-core>=0.1,<0.2"]
[project.entry-points."bias.extensions"]
approval = "bias_ext_approval.backend.ext:extend"
[tool.setuptools.packages.find]
where = ["."]
include = ["bias_ext_approval*"]
